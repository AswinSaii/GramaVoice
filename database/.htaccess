# Protect database directory
Order deny,allow
Deny from all

# Prevent directory browsing
Options -Indexes

# Block access to all files in this directory
<Files "*">
    Order deny,allow
    Deny from all
</Files>
